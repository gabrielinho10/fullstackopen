
---

## 📄 0.6.md

```markdown
# 0.6: Nueva nota en SPA

```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: Usuario escribe una nota y hace clic en Save

    Note right of browser: JavaScript captura el evento y evita recarga

    browser->>browser: Actualiza la lista de notas

    browser->>server: POST /new_note_spa (JSON)
    activate server
    server-->>browser: 201 Created
    deactivate server